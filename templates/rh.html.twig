{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid mt-3">
        <h2>Création de compte</h2>
        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
